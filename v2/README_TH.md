# Attendance Online PWA v2

เวอร์ชันนี้เพิ่มระบบตามรายการ:
- รีเฟรชแล้วไม่หลุด login ด้วย localStorage session
- พนักงานรายชั่วโมง / รายวัน / รายเดือน
- รอบจ่ายรายเดือน / ราย 14 วัน
- สวัสดิการตามวันทำงาน หรือ Fix รายเดือน
- ปฏิทินบริษัท
- วันลา + อนุมัติ
- OT request + อนุมัติเท่านั้นถึงคิดเงิน
- กะงาน / shift
- ประวัติการทำงานของ user
- Payroll Slip สำหรับพิมพ์
- Export CSV รายละเอียดมากขึ้น
- ลบข้อมูลเวลาเข้าออก และเคลียร์ audit log
- แสดงเวลาเข้าออก 1 คน 1 รายการต่อวัน
- UI ใหม่แบบ responsive

## วิธีอัปเดตโดยไม่ทับของเดิม
แนะนำสร้างโฟลเดอร์ `v2/` ใน GitHub แล้วอัปโหลดไฟล์ชุดนี้เข้าไป
จากนั้นเปิดเว็บที่:
`https://tanupon66.github.io/attendance-online/v2/`

ถ้าจะทับของเดิม ให้อัปโหลดไฟล์ทั้งหมดไว้ root ของ repo แทน

## Firebase
ใช้ Firestore + Anonymous Auth เท่านั้น ไม่ใช้ Firebase Storage
รูปถูกบีบอัดและเก็บเป็น Base64 ใน Firestore

## หลังอัปโหลด
เปิด URL พร้อม version เช่น `?v=2` และล้าง cache ถ้ายังเห็นหน้าเก่า

## อัปเดต v2.1.0
- แก้หน้า Admin โหลดพนักงานไม่ขึ้นเมื่อบางโมดูล error
- เพิ่มปุ่มลบพนักงาน
- แก้ระบบลบข้อมูลเวลาเข้าออกและเคลียร์ Audit Log ให้ลบเป็นชุดจนหมด
- ปรับการอ่าน attendance ให้รองรับข้อมูลเก่าที่บาง field ไม่ครบ
- ต้องใช้ Firestore Rules V2.1 ใน docs/FIRESTORE_RULES_V2.txt ถ้าต้องการลบข้อมูล
